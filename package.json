{
  "name": "sql-parser",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Vasiliy Varyukhin",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('build', { recursive: true });\" && pegjs --optimize size -o ./build/parser.js sssql.pegjs && parcel build ./build/parser.js --no-source-maps --no-cache",
    "dev": "",
    "test": "jest"
  },
  "engines": {
    "node": ">=10.12.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "parcel-bundler": "1.12.3",
    "pegjs": "^0.10.0"
  }
}
