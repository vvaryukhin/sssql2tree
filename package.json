{
  "name": "sql-parser",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Vasiliy Varyukhin",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "build": "node -e \"require('fs').mkdirSync('build', { recursive: true });\" && pegjs --optimize size -o ./build/parser.js sssql.pegjs"
  },
  "engines": {
    "node": ">=10.12.0"
  },
  "devDependencies": {
    "pegjs": "^0.10.0"
  }
}
