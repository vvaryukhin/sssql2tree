{
  "name": "sssql2tree",
  "version": "1.0.0",
  "main": "index.js",
  "types": "index.d.ts",
  "author": "Vasiliy Varyukhin",
  "license": "MIT",
  "dependencies": {},
  "scripts": {
    "generate": "pegjs --optimize size -o ./src/parser.js ./src/sssql.pegjs",
    "build": "npm run generate && parcel build ./src/parser.js --no-source-maps --no-cache",
    "test": "jest"
  },
  "engines": {
    "node": ">=10.12.0"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "parcel-bundler": "1.12.3",
    "pegjs": "^0.10.0",
    "typescript": "^4.2.3"
  }
}
